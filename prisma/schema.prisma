generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  // url      = "mysql://root:pass%403264@localhost:3306/hospital_management"
  url      = "mysql://schoolAttendance:Sensarsoft%40123@[2a02:4780:12:f6a7::1]:3306/hospital_management"

  // // url      = env("DATABASE_URL")
  //    url      = env("DATABASE_URL")
}

enum HospitalStatus {
  ACTIVE
  INACTIVE
}

enum AdminStatus {
  ACTIVE
  INACTIVE
}

enum PatientStatus {
  ACTIVE
  INACTIVE
}

model Hospital {
  id                    Int                          @id @default(autoincrement()) //  @default(autoincrement())
  name                  String
  address               String
  photo                 String                       @default("https://img.freepik.com/premium-vector/hospital-logo-design-vector-illustration-white-background_272204-8968.jpg?w=2000")
  HospitalStatus        HospitalStatus               @default(ACTIVE)
  phone                 String
  mail                  String
  Users                 User[]
  Admins                Admin[]
  Patients              Patient[]
  Consultation          Consultation[]
  Treatments            Treatment[]
  Medicians             Medician[]
  Injections            Injection[]
  Tonics                Tonic[]
  TestingAndScannings   TestingAndScanningPatient[]
  TestingAndScanningHs  TestingAndScanningHospital[]
  RoomsAvailables       RoomsAvailable[]
  Payments              Payment[]
  MedicineAndInjections MedicineAndInjection[]
  Fees                  Fees[]
  MedicinePatients      MedicinePatient[]
  InjectionPatients     InjectionPatient[]
  TonicPatients         TonicPatient[]
  ScanAndTests          ScanAndTest[]
  Drawers               Drawer[]
  IncomeAndExpenses     IncomeAndExpense[]
  cosmetics             cosmetic[]
  ButtonPermissions     ButtonPermission[]
  Tickets              Tickets[]
}

model User {
  id           Int       @id @default(autoincrement())
  hospital_Id  Int
  user_Id      String
  password     String
  role         String
  device_Id    String?   @db.Text
  sessionToken String?   @db.Text
  isLoggedIn   Boolean   @default(false)
  Admin        Admin[]
  Patient      Patient[]
  Hospital     Hospital  @relation(fields: [hospital_Id], references: [id])

  @@unique([hospital_Id, user_Id])
}

model Admin {
  id           Int            @id @default(autoincrement())
  hospital_Id  Int
  user_Id      String
  name         String
  designation  String?
  role         String // e.g., Doctor, Nurse, Admin
  doctorAmount Float?         @default(0)
  specialist   String?
  permissions  Json? // store [1,2,3] etc for Medical Staff; keep NULL for ADMIN
  phone        String
  email        String
  address      String
  photo        String?        @default("https://as2.ftcdn.net/v2/jpg/05/89/93/27/1000_F_589932782_vQAEAZhHnq1QCGu5ikwrYaQD0Mmurm0N.jpg")
  status       AdminStatus    @default(ACTIVE)
  gender       String 
  createdAt    DateTime       @default(now())
  updatedAt    DateTime       @updatedAt
  Consultation Consultation[]
  Doctor       Treatment[]    @relation("Doctor_Treatment")
  Staff        Treatment[]    @relation("Staff_Treatment")
  Hospital     Hospital       @relation(fields: [hospital_Id], references: [id])
  User         User           @relation(fields: [hospital_Id, user_Id], references: [hospital_Id, user_Id])

  @@unique([hospital_Id, user_Id])
}

model ButtonPermission {
  id          Int      @id @default(autoincrement())
  hospital_Id Int
  key         String   @unique
  designation String? // optional template (Doctor, Nurse, etc.)
  defaultIds  Json? // optional default permission IDs for designation
  Hospital    Hospital @relation(fields: [hospital_Id], references: [id])
}

model AdminStrator {
  id          Int     @id @default(autoincrement())
  user_Id     String
  name        String
  status      String
  role        String
  designation String? // was 'destination'
  phone       String
  email       String
  photo       String? @default("https://img.freepik.com/premium-vector/technology-concept-vector-illustration-featuring-consulting-design-flat-style-elements_1226483-4088.jpg?semt=ais_hybrid&w=740&q=80")
  address     String
  gender      String

  @@map("administrator")
}

model Patient {
  id             Int           @id @default(autoincrement())
  hospital_Id    Int
  user_Id        String?
  staff_Id       String
  name           String
  ac_name        String?
  phone          Json
  email          Json
  photo          String?       @default("null")
  status         PatientStatus @default(ACTIVE)
  address        Json?
  dob            DateTime
  gender         String?
  bldGrp         String?
  currentProblem String?
  medicalHistory String?
  tempCreatedAt  DateTime?
  createdAt      String?
  updatedAt      String?

  Consultation         Consultation[]
  Treatment            Treatment[]
  MedicineAndInjection MedicineAndInjection[]      @relation("Patient_MedicineAndInjection")
  TestingAndScanning   TestingAndScanningPatient[]
  Payments             Payment[]
  MedicinePatients     MedicinePatient[]
  InjectionPatients    InjectionPatient[]
  TonicPatients        TonicPatient[]

  Hospital Hospital @relation(fields: [hospital_Id], references: [id])
  User     User?     @relation(fields: [hospital_Id, user_Id], references: [hospital_Id, user_Id])

  //@@unique([hospital_Id, user_Id])
}

enum Status {
  SCHEDULED
  COMPLETED
  CANCELLED
  ENDPROCESS
  ENDPROCESSING
  ONGOING
  PENDING
}

enum QueueStatus {
  PENDING
  ONGOING
  DRQUEUE
  COMPLETED
  CANCELLED
}

model Consultation {
  id              Int         @id @default(autoincrement())
  hospital_Id     Int
  patient_Id      Int
  doctor_Id       String
  purpose         String
  symptoms        Boolean     @default(false)
  notes           Json?
  cancelReason    String?
  treatment       Boolean     @default(false)
  height          Float?
  weight          Float?
  bp              String?
  sugar           String?
  temperature     Float?
  medicineTonic   Boolean     @default(false)
  Injection       Boolean     @default(false)
  scanningTesting Boolean     @default(false)
  status          Status      @default(PENDING)
  queueStatus     QueueStatus @default(PENDING)
  access          Json?
  createdAt       String?
  updatedAt       String?
  paymentStatus   Boolean     @default(false)

  Hospital                  Hospital                    @relation(fields: [hospital_Id], references: [id])
  // Patient                   Patient                     @relation(fields: [hospital_Id, patient_Id], references: [hospital_Id, user_Id])
  Patient                   Patient                     @relation(fields: [patient_Id], references: [id])
  Doctor                    Admin                       @relation(fields: [hospital_Id, doctor_Id], references: [hospital_Id, user_Id])
  MedicinePatient           MedicinePatient[]
  InjectionPatient          InjectionPatient[]
  TonicPatient              TonicPatient[]
  Payment                   Payment[]
  TeatingAndScanningPatient TestingAndScanningPatient[]
}

model Treatment {
  id             Int      @id @default(autoincrement())
  hospital_Id    Int
  staff_Id       Json // Array/object
  patient_Id     Int
  doctor_Id      Json // Array/object
  treatmentName  Json? // Array/object
  startDate      String
  endDate        String
  progress       String?
  treatmentNotes Json?
  status         Status   @default(PENDING)
  createdAt      DateTime @default(now())
  paymentStatus  Boolean  @default(false)
  Hospital       Hospital @relation(fields: [hospital_Id], references: [id])
  //Patient        Patient  @relation(fields: [hospital_Id, patient_Id], references: [hospital_Id, user_Id])
    Patient                   Patient                     @relation(fields: [patient_Id], references: [id])
  Doctor         Admin[]  @relation("Doctor_Treatment")
  Staff          Admin[]  @relation("Staff_Treatment")
}

model Medician {
  id                   Int                    @id @default(autoincrement())
  hospital_Id          Int
  medicianName         String
  medicianCode         String? @unique
  stock                Int?
  amount               Float
  expiryDate           String? 
  manifacturingDate    String?
  staff_Id             Json? // Array/object
  MedicineAndInjection MedicineAndInjection[] @relation("MedicineAndInjection_Medician")
  Hospital             Hospital               @relation(fields: [hospital_Id], references: [id])
  MedicinePatients     MedicinePatient[]
}

// @@unique([medicianName, hospital_Id])
model Tonic {
  id                   Int                    @id @default(autoincrement())
  hospital_Id          Int
  tonicName            String
  tonicCode            String? @unique
  stock                Json? // Quantity 250ml=stack=50, 500ml etc. 100ml=50
  amount               Json?
  staff_Id             Json? // Array/object
  expiryDate           String?
  manifacturingDate    String?
  MedicineAndInjection MedicineAndInjection[] @relation("Tonic_MedicineAndInjection")
  Hospital             Hospital               @relation(fields: [hospital_Id], references: [id])
  TonicPatients        TonicPatient[]
}

model Injection {
  id                   Int                    @id @default(autoincrement())
  hospital_Id          Int
  injectionName        String
  injectionCode        String? @unique
  stock                Json?
  unit                 String? // ✅ Added: mg, mL
  amount               Json?
  expiryDate           String?
  manifacturingDate    String?
  staff_Id             Json? // Array/object
  MedicineAndInjection MedicineAndInjection[] @relation("Injection_MedicineAndInjection")
  Hospital             Hospital               @relation(fields: [hospital_Id], references: [id])
  InjectionPatients    InjectionPatient[]
}

model cosmetic {
  id          Int      @id @default(autoincrement())
  hospital_Id Int
  name        String
  type        String? // consider enum if limited set
  unit        String?
  imageUrl      String? @db.Text
  stock       Int      @default(0)
  amount      Float
  staff_Id    Int
  Hospital    Hospital @relation(fields: [hospital_Id], references: [id])
}

model TestingAndScanningHospital {
  id          Int      @id @default(autoincrement())
  hospital_Id Int
  type        String // consider enum if limited set
  status      String // consider enum
  roomNo      String
  staff_Id    Int
  amount      Float
  Hospital    Hospital @relation(fields: [hospital_Id], references: [id])
}

model MedicineAndInjection {
  id          Int    @id @default(autoincrement())
  hospital_Id Int
  patient_Id  Int
  doctor_Id   Json // Array/object
  staff_Id    Json? // Array/object

  medicine_Id       Json? // Array/object
  dosageMedicine    Json?
  medicineNotes     Json?
  frequencyMedicine Json?
  durationMedicine  Json?
  medicineStatus    Boolean @default(false) // consider enum

  injection_Id       Json?
  dosageInjection    Json?
  InjectionNotes     Json?
  frequencyInjection Json?
  durationInjection  Json?
  injectionStatus    Boolean @default(false)

  status        Status      @default(PENDING) // consider enum
  paymentStatus Boolean     @default(false)
  createdAt     DateTime    @default(now())
  Hospital      Hospital    @relation(fields: [hospital_Id], references: [id])
  //Patient       Patient     @relation("Patient_MedicineAndInjection", fields: [patient_Id, hospital_Id], references: [user_Id, hospital_Id])
  Patient                   Patient                     @relation("Patient_MedicineAndInjection",fields: [patient_Id], references: [id])
  Medician      Medician[]  @relation("MedicineAndInjection_Medician")
  Injection     Injection[] @relation("Injection_MedicineAndInjection")
  Tonic         Tonic[]     @relation("Tonic_MedicineAndInjection")
}

enum TestingAndScanningStatus {
  SCHEDULED
  COMPLETED
  CANCELLED
  PENDING
}

// model TestingAndScanningPatient {
//   id            Int                      @id @default(autoincrement())
//   hospital_Id   Int
//   patient_Id    String
//   doctor_Id     Json
//   staff_Id      Json?
//   // levels        Levels?
//   title         String?
//   scheduleDate  DateTime
//   selectedOptions Json?
//   type          String?
//   status        TestingAndScanningStatus @default(PENDING) // consider enum
//   paymentStatus Boolean                  @default(false)
//   result        String?
//   createdAt     String    
//   updatedAt String?           
//   Hospital      Hospital                 @relation(fields: [hospital_Id], references: [id])
//   Patient       Patient                  @relation(fields: [patient_Id, hospital_Id], references: [user_Id, hospital_Id])
// }

enum PaymentType {
  ManualPay
  OnlinePay
}

enum PaymentStatus {
  PENDING
  PAID
  FAILED
  REFUNDED
}

enum Type {
  REGISTRATIONFEE
  TESTINGFEESANDSCANNINGFEE
  TREATMENTFEE
  MEDICINEFEEANDINJECTIONFEE
  MEDICINETONICINJECTIONFEES
  ROOMCHARGE
  OTHER
}

// model Payment {
//   id            Int           @id @default(autoincrement())
//   hospital_Id   Int
//   patient_Id    String
//   staff_Id     String?
//   reason        String
//   status        PaymentStatus @default(PENDING)
//   amount        Float?
//   paymentType          PaymentType?
//   type          Type?
//   transactionId Json?
//   billingId     String?
//   createdBy     String?
//   updategdBy    String?
//   createdAt     String
//   updatedAt     String?
//   Hospital      Hospital      @relation(fields: [hospital_Id], references: [id])
//   Patient       Patient       @relation(fields: [patient_Id, hospital_Id], references: [user_Id, hospital_Id])
// }

model Payment {
  id                         Int                         @id @default(autoincrement())
  hospital_Id                Int
  patient_Id                 Int
  staff_Id                   String?
  consultation_Id            Int?
  reason                     String
  status                     PaymentStatus               @default(PENDING)
  amount                     Float?
  paymentType                PaymentType?
  type                       Type?
  transactionId              Json?
  billingId                  String?
  createdBy                  String?
  updategdBy                 String?
  createdAt                  String
  updatedAt                  String?
  Hospital                   Hospital                    @relation(fields: [hospital_Id], references: [id])
  //Patient                    Patient                     @relation(fields: [patient_Id, hospital_Id], references: [user_Id, hospital_Id])
  Patient                   Patient                     @relation(fields: [patient_Id], references: [id])

  TestingAndScanningPatients TestingAndScanningPatient[]
  MedicinePatient            MedicinePatient[]
  InjectionPatient           InjectionPatient[]
  TonicPatient               TonicPatient[]
  Consultation               Consultation?               @relation(fields: [consultation_Id], references: [id])
}

model RoomsAvailable {
  id          Int      @id @default(autoincrement())
  hospital_Id Int
  staff_Id    String
  roomNo      String
  capacity    Int
  notes       String
  type        String // consider enum if limited set // e.g., ICU, General, Private
  amount      Float
  Hospital    Hospital @relation(fields: [hospital_Id], references: [id])
}

model Fees {
  id          Int      @id @default(autoincrement())
  hospital_Id Int
  type        String?
  amount      Float?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  Hospital    Hospital @relation(fields: [hospital_Id], references: [id])
}

model MedicinePatient {
  id              Int     @id @default(autoincrement())
  hospital_Id     Int
  patient_Id      Int
  doctor_Id       String
  medicine_Id     Int
  consultation_Id Int?
  reason          String?
  quantity        Float
  reduceQuantity  Float?
  afterEat        Boolean
  morning         Boolean @default(false)
  afternoon       Boolean @default(false)
  night           Boolean @default(false)
  days            Int?    @default(0)
  reduceDays      Int?    @default(0)
  quantityNeeded  Float?
  tabletsCharge   Int?
  total           Float?
  status          Status  @default(PENDING)
  paymentStatus   Boolean @default(false)
  createdAt       String?
  updatedAt       String?

  payment_Id       Int?
  Payment          Payment?           @relation(fields: [payment_Id], references: [id])
  Hospital         Hospital           @relation(fields: [hospital_Id], references: [id])
  //Patient          Patient            @relation(fields: [patient_Id, hospital_Id], references: [user_Id, hospital_Id])
    Patient                   Patient                     @relation(fields: [patient_Id], references: [id])
  Medician         Medician           @relation(fields: [medicine_Id], references: [id])
  Consultation     Consultation?      @relation(fields: [consultation_Id], references: [id])
  MedicineProgress MedicineProgress[]
}

model InjectionPatient {
  id              Int     @id @default(autoincrement())
  hospital_Id     Int
  patient_Id      Int
  doctor_Id       String
  injection_Id    Int
  consultation_Id Int?
  reason          String?
  quantity        Int
  Doase           String?
  morning         Boolean @default(false)
  afternoon       Boolean @default(false)
  night           Boolean @default(false)
  days            Int?    @default(0)
  status          Status  @default(PENDING)
  total           Float?
  paymentStatus   Boolean @default(false)
  createdAt       String?
  updatedAt       String?

  payment_Id        Int?
  Payment           Payment?            @relation(fields: [payment_Id], references: [id])
  Hospital          Hospital            @relation(fields: [hospital_Id], references: [id])
  //Patient           Patient             @relation(fields: [patient_Id, hospital_Id], references: [user_Id, hospital_Id])
    Patient                   Patient                     @relation(fields: [patient_Id], references: [id])
  Injection         Injection           @relation(fields: [injection_Id], references: [id])
  Consultation      Consultation?       @relation(fields: [consultation_Id], references: [id])
  InjectionProgress InjectionProgress[]
}

model TonicPatient {
  id              Int     @id @default(autoincrement())
  hospital_Id     Int
  patient_Id      Int
  doctor_Id       String
  tonic_Id        Int
  consultation_Id Int?
  reason          String?
  quantity        Int
  reduceQuantity  Float?
  Doase           String?
  afterEat        Boolean
  morning         Boolean @default(false)
  afternoon       Boolean @default(false)
  night           Boolean @default(false)
  days            Int     @default(0)
  total           Float?
  status          Status  @default(PENDING)
  paymentStatus   Boolean @default(false)
  createdAt       String?
  updatedAt       String?

  payment_Id    Int?
  Payment       Payment?        @relation(fields: [payment_Id], references: [id])
  Hospital      Hospital        @relation(fields: [hospital_Id], references: [id])
  //Patient       Patient         @relation(fields: [patient_Id, hospital_Id], references: [user_Id, hospital_Id])
    Patient                   Patient                     @relation(fields: [patient_Id], references: [id])
  Tonic         Tonic           @relation(fields: [tonic_Id], references: [id])
  Consultation  Consultation?   @relation(fields: [consultation_Id], references: [id])
  TonicProgress TonicProgress[]
}

enum Types {
  SCAN
  TEST
}

// model ScanAndTest {
//   id          Int      @id @default(autoincrement())
//   hospital_Id Int
//   title       String
//   options     Json? // e.g., [{"name": "Hemoglobin", "price": 50}, {"name": "Sugar", "price": 100}, {"name": "WBC Count", "price": 60}, {"name": "RBC Count", "price": 60}, {"name": "Platelet Count", "price": 70}, {"name": "Hematocrit", "price": 100}]
//   type        Types
//   amount      Float?
//   crearedAt   String?
//   updatedAt   String?
//   Hospital    Hospital @relation(fields: [hospital_Id], references: [id])
//   references  ScanAndTestUnitReferance[] @relation("TestReferences")
// }
// model ScanAndTestUnitReferance {
//   id          Int      @id @default(autoincrement())
//   optionName  String   // Matches ScanAndTest.options.name, e.g., "Hemoglobin"
//   unit        String   // e.g. "g/dL", "mg/dL", etc.
//   reference   Json     // e.g., { "0_1_MF": "13.5–20.0", "144_216_M": "13.5–16.5", ... }
//   createdAt   DateTime @default(now())
//   updatedAt   DateTime @updatedAt
//   ScanAndTest ScanAndTest[]
// }
model TestingAndScanningPatient {
  id                    Int                      @id @default(autoincrement())
  hospital_Id           Int
  patient_Id            Int
  doctor_Id             Json
  staff_Id              Json?
  title                 String?
  scheduleDate          DateTime
  queueStatus           QueueStatus              @default(PENDING)
  selectedOptions       Json?
  selectedOptionResults Json?
  type                  String?
  status                TestingAndScanningStatus @default(PENDING)
  paymentStatus         Boolean                  @default(false)
  scanImages            Json?
  amount                Float?
  reason                String?
  result                String?
  createdAt             String
  updatedAt             String?
  consultation_Id       Int?
  payment_Id            Int? // link to Payment
  Payment               Payment?                 @relation(fields: [payment_Id], references: [id])
  Hospital              Hospital                 @relation(fields: [hospital_Id], references: [id])
  //Patient               Patient                  @relation(fields: [patient_Id, hospital_Id], references: [user_Id, hospital_Id])
    Patient                   Patient                     @relation(fields: [patient_Id], references: [id])
  Consultation          Consultation?            @relation(fields: [consultation_Id], references: [id])
}

model ScanAndTest {
  id          Int      @id @default(autoincrement())
  hospital_Id Int
  title       String
  options     Json? // list of sub-tests [{ name, price }]
  type        Types
  amount      Float?
  createdAt   String?
  updatedAt   String?
  Hospital    Hospital @relation(fields: [hospital_Id], references: [id])
}

model ScanAndTestUnitReferance {
  id         Int      @id @default(autoincrement())
  optionName String   @unique // e.g. "Hemoglobin", "ALT", "Creatinine"
  unit       String // e.g. "g/dL", "mg/dL", "U/L"
  referance  Json // JSON of ranges (age/gender/pregnancy keys)
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
}

model Drawer {
  id          Int      @id @default(autoincrement())
  hospital_Id Int
  admin_Id    String?
  reason      String
  amount      Float
  type        String
  note        String?
  createdAt   String
  updatedAt   String?
  Hospital    Hospital @relation(fields: [hospital_Id], references: [id])
}

model IncomeAndExpense {
  id          Int      @id @default(autoincrement())
  hospital_Id Int
  admin_Id    String?
  reason      String
  amount      Float
  type        String
  note        String?
  createdAt   String
  updatedAt   String?
  Hospital    Hospital @relation(fields: [hospital_Id], references: [id])
}

model MedicineProgress {
  id                Int      @id @default(autoincrement())
  medicinePatientId Int
  date              DateTime
  morningTaken      Boolean  @default(false)
  afternoonTaken    Boolean  @default(false)
  nightTaken        Boolean  @default(false)

  morningEmailSent   Boolean @default(false)
  afternoonEmailSent Boolean @default(false)
  nightEmailSent     Boolean @default(false)

  MedicinePatient MedicinePatient @relation(fields: [medicinePatientId], references: [id])

  @@index([medicinePatientId])
}

model TonicProgress {
  id             Int      @id @default(autoincrement())
  tonicPatientId Int
  date           DateTime
  morningTaken   Boolean  @default(false)
  afternoonTaken Boolean  @default(false)
  nightTaken     Boolean  @default(false)

  morningEmailSent   Boolean @default(false)
  afternoonEmailSent Boolean @default(false)
  nightEmailSent     Boolean @default(false)

  TonicPatient TonicPatient @relation(fields: [tonicPatientId], references: [id])

  @@index([tonicPatientId])
}

model InjectionProgress {
  id                 Int      @id @default(autoincrement())
  injectionPatientId Int
  date               DateTime
  morningTaken       Boolean  @default(false)
  afternoonTaken     Boolean  @default(false)
  nightTaken         Boolean  @default(false)

  morningEmailSent   Boolean @default(false)
  afternoonEmailSent Boolean @default(false)
  nightEmailSent     Boolean @default(false)

  InjectionPatient InjectionPatient @relation(fields: [injectionPatientId], references: [id])

  @@index([injectionPatientId])
}

model Tickets{
  id Int @id @default(autoincrement())
  hospital_Id Int
  admin_Id String
  title String
  description String
  status Status
  createdAt String
  updatedAt String?
  Hospital    Hospital @relation(fields: [hospital_Id], references: [id])
}
